# ─────────────────────────────────────────────────────
#  Fuad Portfolio Backend — Environment Configuration
# ─────────────────────────────────────────────────────

# Server
PORT=5000
NODE_ENV=development

# PostgreSQL — use your actual credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=portfolio_db
DB_USER=postgres
DB_PASSWORD=

# JWT
JWT_SECRET=your_super_long_and_random_jwt_secret_here_min_32_chars
JWT_EXPIRES_IN=7d

# Admin credentials (used during db:seed)
ADMIN_EMAIL=fuadmohammed368@gmail.com
ADMIN_PASSWORD=fuadKOKO5991#
ADMIN_NAME=Fuad Mohammed

# Email (nodemailer — Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_gmail_app_password
CONTACT_RECIPIENT=fuad@hijrabank.et

# CORS — frontend URL(s) comma-separated
ALLOWED_ORIGINS=http://localhost:3000,https://fuadmohammed.dev

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
